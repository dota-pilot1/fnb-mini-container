<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cj.fnbmini.auth.AuthMapper">

    <select id="findByUserId" resultType="com.cj.fnbmini.auth.entity.AppUser">
        SELECT id, user_id, password, username, created_at
        FROM app_user
        WHERE user_id = #{userId}
    </select>

    <select id="countByUserId" resultType="int">
        SELECT COUNT(*) FROM app_user WHERE user_id = #{userId}
    </select>

    <insert id="insertUser" parameterType="com.cj.fnbmini.auth.entity.AppUser">
        INSERT INTO app_user (user_id, password, username)
        VALUES (#{userId}, #{password}, #{username})
    </insert>

</mapper>
